const heading = React.createElement("h1", {}, "Hello Word Namaste React");  //Create Element
const root = ReactDOM.createRoot(document.getElementById("root")); //Create Root
root.render(heading); //Render heading element in root